import {useCallback,useEffect,useState} from "react";import {FiActivity,FiSearch} from "react-icons/fi";import Loader from "../../components/Loader";import {getAuditLogs} from "../../services/api";
export default function AuditLogs(){const[data,setData]=useState({items:[],page:1,pages:0,total:0}),[search,setSearch]=useState(""),[page,setPage]=useState(1),[loading,setLoading]=useState(true),[error,setError]=useState("");const load=useCallback(async()=>{setLoading(true);setError("");try{setData((await getAuditLogs({search:search||undefined,page,page_size:30})).data)}catch(e){setError(e.response?.data?.detail||"Could not load audit logs")}finally{setLoading(false)}},[search,page]);useEffect(()=>{const id=setTimeout(load,search?300:0);return()=>clearTimeout(id)},[load,search]);return <div className="space-y-6"><div><h1 className="page-title flex items-center gap-2"><FiActivity/>Admin Audit Logs</h1><p className="mt-1 text-sm text-slate-500">Immutable history of sensitive administrator actions.</p></div><label className="relative block max-w-lg"><FiSearch className="absolute left-3 top-3 text-slate-400"/><input className="input-field pl-10" placeholder="Search actions, targets, or administrators" aria-label="Search audit logs" value={search} onChange={e=>{setSearch(e.target.value);setPage(1)}}/></label>{error&&<div role="alert" className="rounded-lg bg-red-50 p-4 text-red-700 dark:bg-red-950/40 dark:text-red-200">{error}</div>}{loading?<Loader/>:<div className="overflow-hidden rounded-lg border border-slate-200 bg-white dark:border-slate-700 dark:bg-slate-900"><div className="overflow-x-auto"><table className="w-full min-w-[850px] text-left text-sm"><thead className="bg-slate-50 text-slate-500 dark:bg-slate-800"><tr><th className="px-4 py-3">Date</th><th className="px-4 py-3">Administrator</th><th className="px-4 py-3">Action</th><th className="px-4 py-3">Target</th><th className="px-4 py-3">Description</th></tr></thead><tbody className="divide-y divide-slate-100 dark:divide-slate-800">{data.items.map(x=><tr key={x.id}><td className="px-4 py-3 whitespace-nowrap">{new Date(x.created_at).toLocaleString()}</td><td className="px-4 py-3">{x.actor_name}</td><td className="px-4 py-3 font-mono text-xs">{x.action}</td><td className="px-4 py-3 capitalize">{x.target_type} {x.target_id?`#${x.target_id}`:""}</td><td className="max-w-md px-4 py-3 text-slate-600 dark:text-slate-300">{x.description||"-"}</td></tr>)}</tbody></table></div>{!data.items.length&&<p className="p-10 text-center text-slate-500">No audit entries found.</p>}<div className="flex items-center justify-between border-t border-slate-200 p-3 text-sm dark:border-slate-700"><span>{data.total} entries</span><div className="flex gap-2"><button className="btn-secondary px-3 py-1.5" disabled={page<=1} onClick={()=>setPage(page-1)}>Previous</button><button className="btn-secondary px-3 py-1.5" disabled={page>=data.pages} onClick={()=>setPage(page+1)}>Next</button></div></div></div>}</div>}
